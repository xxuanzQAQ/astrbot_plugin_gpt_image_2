{
  "api_base_url": {
    "description": "API Base URL",
    "type": "string",
    "hint": "默认使用 https://api.apimart.ai/v1；也兼容完整接口 URL，例如 https://api.apimart.ai/v1/images/generations。支持 OpenAI/NewAPI 风格同步返回 b64_json，例如 https://newapi-hk.qianye.host/v1。",
    "default": "https://api.apimart.ai/v1"
  },
  "api_key": {
    "description": "API Key",
    "type": "string",
    "hint": "Bearer Token；可填写 APIMart 或兼容站点的 API Key。APIMart 注册链接：👉 https://apimart.ai/register?aff=J3ZjCO",
    "obvious_hint": true,
    "default": ""
  },
  "model": {
    "description": "模型名称",
    "type": "string",
    "hint": "默认 gpt-image-2。若未在命令中指定模型，插件遇到 ChatGPT 上游鉴权失败时会自动尝试 gpt-image-1 兼容参数。",
    "default": "gpt-image-2"
  },
  "default_size": {
    "description": "默认图片比例",
    "type": "string",
    "hint": "支持 auto、1:1、3:2、2:3、4:3、3:4、5:4、4:5、16:9、9:16、2:1、1:2、3:1、1:3、21:9、9:21，或像素尺寸如 1881x836。",
    "options": [
      "auto",
      "1:1",
      "3:2",
      "2:3",
      "4:3",
      "3:4",
      "5:4",
      "4:5",
      "16:9",
      "9:16",
      "2:1",
      "1:2",
      "3:1",
      "1:3",
      "21:9",
      "9:21"
    ],
    "default": "1:1"
  },
  "default_resolution": {
    "description": "默认输出分辨率档位",
    "type": "string",
    "hint": "4k 仅支持 16:9 / 9:16 / 2:1 / 1:2 / 3:1 / 1:3 / 21:9 / 9:21。",
    "options": [
      "1k",
      "2k",
      "4k"
    ],
    "default": "1k"
  },
  "include_resolution": {
    "description": "请求中附带 resolution 字段",
    "type": "bool",
    "hint": "通常保持开启；插件会按模型自动判断，并在接口报 resolution 参数错误时自动去掉重试。关闭后永不发送该字段。",
    "default": true
  },
  "official_fallback": {
    "description": "启用官方渠道兜底",
    "type": "bool",
    "hint": "默认关闭；开启后请求体会传 official_fallback=true。",
    "default": false
  },
  "auto_wait": {
    "description": "提交后自动等待结果",
    "type": "bool",
    "hint": "异步接口开启后会轮询 /v1/tasks/{task_id}，完成后直接发送图片；同步返回图片数据的接口不需要轮询。",
    "default": true
  },
  "initial_delay": {
    "description": "首次查询延迟",
    "type": "int",
    "hint": "提交任务后等待多少秒再第一次查询，建议 10~20 秒。",
    "default": 12
  },
  "poll_interval": {
    "description": "轮询间隔",
    "type": "int",
    "hint": "每次查询任务状态的间隔秒数，建议 3~5 秒。",
    "default": 5
  },
  "poll_timeout": {
    "description": "轮询超时",
    "type": "int",
    "hint": "自动等待图片结果的最长秒数。",
    "default": 180
  },
  "request_timeout": {
    "description": "HTTP 请求超时",
    "type": "int",
    "hint": "单次提交或查询请求的超时时间，单位秒。若接口返回 HTTP 504，通常是 NewAPI/反代/上游通道先超时，还需要在站点侧调高超时。",
    "default": 60
  },
  "transient_retries": {
    "description": "临时上游错误重试次数",
    "type": "int",
    "hint": "提交图片时遇到 502/503/504、context deadline exceeded、poll error 等临时错误会自动重试。",
    "default": 2
  },
  "transient_retry_delay": {
    "description": "临时错误重试间隔",
    "type": "int",
    "hint": "每次临时上游错误重试前等待的秒数。",
    "default": 5
  },
  "newapi_log_lookup": {
    "description": "NewAPI 504 后查询后台日志",
    "type": "bool",
    "hint": "开启后，同步生图接口提交遇到 HTTP 504 时，会尝试查询 NewAPI /api/log/self 和 /api/log/self/search，找到后台最终错误后直接返回该错误。",
    "default": true
  },
  "newapi_log_key": {
    "description": "NewAPI 日志接口鉴权 Key",
    "type": "string",
    "hint": "可填写 NewAPI 后台日志接口需要的 New-Api-User 或用户鉴权 Token。留空时默认尝试使用 api_key。",
    "default": ""
  },
  "newapi_log_lookup_timeout": {
    "description": "NewAPI 日志查询超时",
    "type": "int",
    "hint": "HTTP 504 后等待后台日志落库的最长秒数。",
    "default": 45
  },
  "newapi_log_lookup_interval": {
    "description": "NewAPI 日志查询间隔",
    "type": "int",
    "hint": "HTTP 504 后轮询后台日志的间隔秒数。",
    "default": 5
  },
  "max_reference_images": {
    "description": "最多参考图数量",
    "type": "int",
    "hint": "gpt-image-2 参考图上限按接口能力处理，插件最多保留 16 张，超过会被截断。",
    "default": 16
  },
  "include_result_link": {
    "description": "结果附带图片链接",
    "type": "bool",
    "hint": "开启后发送图片的同时附带结果 URL，方便转存。",
    "default": true
  },
  "debug_log_payload": {
    "description": "记录调试请求日志",
    "type": "bool",
    "hint": "开启后在日志中记录脱敏后的请求 payload、响应和参考图提交值；API Key 与 base64 正文不会完整打印。",
    "default": false
  },
  "image_to_image_endpoint": {
    "description": "图生图接口路径",
    "type": "string",
    "hint": "auto 会在 APIMart 使用 /images/generations，在其他兼容站点使用 /images/edits；也可手动填写 /images/generations 或 /images/edits。",
    "default": "auto"
  }
}
